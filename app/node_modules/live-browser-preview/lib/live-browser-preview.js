(function() {
  var live_browser = require('../build/Release/live-browser-preview.node');

  exports.openLiveBrowser = function(command, args, enableRemoteDebugging, appSupportDir) {
    return live_browser.openLiveBrowser(command, args, enableRemoteDebugging, appSupportDir);
  };
    
  exports.closeLiveBrowser = function(callback) {
    return live_browser.closeLiveBrowser(callback);
  };

}).call(this);
