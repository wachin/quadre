#!/bin/sh
basedir=`dirname "$0"`

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../node-cpplint/bin/cpplint" "$@"
  ret=$?
else 
  node  "$basedir/../node-cpplint/bin/cpplint" "$@"
  ret=$?
fi
exit $ret
